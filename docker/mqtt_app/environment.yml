# run `conda env create -f environment.yml` to create and install the dependencies

name: tag
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9.16
  - paho-mqtt=1.6.1
  - requests=2.28.1
  - pip:
    - influxdb-client==1.36.1
    